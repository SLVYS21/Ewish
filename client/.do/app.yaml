name: ewish
static_sites:
- name: client
  source_dir: client
  build_command: npm install && npm run build
  output_dir: dist
  index_document: index.html
  error_document: index.html
  routes:
  - path: /